import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FolderDetailPage has title', ( tester) async {
    //  await tester.pumpWidget(buildTestableWidget
    //(FolderDetailPage(title: 'FolderDetail')));
    //  final titleFinder = find.text('FolderDetail');
    //  expect(titleFinder, findsOneWidget);
  });
}
