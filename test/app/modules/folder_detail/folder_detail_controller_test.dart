import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:cloudged/app/modules/folder_detail/folder_detail_module.dart';

void main() {
  initModule(FolderDetailModule());
  // FolderDetailController folderdetail;
  //
  setUp(() {
    //     folderdetail = FolderDetailModule.to.get<FolderDetailController>();
  });

  group('FolderDetailController Test', () {
    //   test("First Test", () {
    //     expect(folderdetail, isInstanceOf<FolderDetailController>());
    //   });

    //   test("Set Value", () {
    //     expect(folderdetail.value, equals(0));
    //     folderdetail.increment();
    //     expect(folderdetail.value, equals(1));
    //   });
  });
}
