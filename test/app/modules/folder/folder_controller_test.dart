import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:cloudged/app/modules/folder/folder_module.dart';

void main() {
  initModule(FolderModule());
  // FolderController folder;
  //
  setUp(() {
    //     folder = FolderModule.to.get<FolderController>();
  });

  group('FolderController Test', () {
    //   test("First Test", () {
    //     expect(folder, isInstanceOf<FolderController>());
    //   });

    //   test("Set Value", () {
    //     expect(folder.value, equals(0));
    //     folder.increment();
    //     expect(folder.value, equals(1));
    //   });
  });
}
