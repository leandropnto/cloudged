import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('FolderPage has title', (tester) async {
    //  await tester.pumpWidget
    //(buildTestableWidget(FolderPage(title: 'Folder')));
    //  final titleFinder = find.text('Folder');
    //  expect(titleFinder, findsOneWidget);
  });
}
